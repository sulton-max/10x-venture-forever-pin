# ForeverPin — Codebase

The code, and the only place code lives. Single-service shape.

- `forever-pin.backend-services/` — .NET (Clean Arch): `forever-pin.backend-services.slnx` + projects (`ForeverPin.Api`, `ForeverPin.Redirect.Api`, `ForeverPin.Common*`, `ForeverPin.Codes`, `ForeverPin.Tests.*`).
- `forever-pin.frontend-services/` — React 19 + Vite + Tailwind v4 app (`@wow-two-beta/ui`, pnpm).
