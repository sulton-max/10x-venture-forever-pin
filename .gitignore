# .NET build output
bin/
obj/
*.user

# Frontend build output served by the Api (regenerate via `pnpm -C engineering/codebase/forever-pin.frontend-services build`)
engineering/codebase/forever-pin.backend-services/ForeverPin.Api/wwwroot/

# Local config overrides (loaded by HostConfiguration; never committed)
appsettings.Local.json

# IDE
.idea/
.vs/
*.DS_Store

# Rider/EF tooling
*.received.*

# Serilog file output (per-service runtime logs — never committed)
engineering/codebase/forever-pin.backend-services/ForeverPin.Api/logs/
engineering/codebase/forever-pin.backend-services/ForeverPin.Redirect.Api/logs/
